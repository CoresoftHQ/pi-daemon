# @coresoft-hq/pi-daemon-contract

The pi-daemon `/v1` wire contract: request and response shapes, the event envelope, runtime
validation schemas, and OpenAPI generated from them. Published, because clients in other
repositories compile against it. Built in milestone M5; a placeholder until then.
