{
  "name": "@coresoft-hq/pi-daemon-contract",
  "version": "0.1.0",
  "description": "The pi-daemon /v1 wire contract: request and response shapes, the event envelope, runtime schemas, and generated OpenAPI. Consumed by the daemon and by every client.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "development": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -b"
  },
  "dependencies": {
    "typebox": "^1.3.7"
  }
}
